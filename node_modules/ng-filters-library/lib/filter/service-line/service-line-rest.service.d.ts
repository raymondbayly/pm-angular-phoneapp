import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { ServiceLineConfigService } from './service-line-config.service';
export declare class ServiceLineRestService {
    private http;
    private serviceLineConfig;
    constructor(http: HttpClient, serviceLineConfig: ServiceLineConfigService);
    /**
     * return all service line descriptions
     */
    getAllServiceLineDescriptions(): Observable<string[]>;
}
