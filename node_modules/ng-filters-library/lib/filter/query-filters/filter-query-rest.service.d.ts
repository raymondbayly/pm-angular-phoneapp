import { Observable } from 'rxjs';
import { HttpClient, HttpParams } from '@angular/common/http';
import { FilterQueryConfigService } from './filter-query-config.service';
export declare class FilterQueryRestService {
    private http;
    private filterQueryConfig;
    private pollInstance$;
    constructor(http: HttpClient, filterQueryConfig: FilterQueryConfigService);
    /**
     * Rest call for getting filtered visits according to current criteria
     */
    getFilteredVisits(queryParamsGetter: any): Observable<string[]>;
    /**
     * get updated filter criteria on an interval
     */
    getFilteredVisitsInterval(queryParamsGetter: any): Observable<string[]>;
    /**
     * returns the filter criteria in the form of a HttpParams object
     */
    getQueryParams(serverFilterCriteria: {}): HttpParams;
}
