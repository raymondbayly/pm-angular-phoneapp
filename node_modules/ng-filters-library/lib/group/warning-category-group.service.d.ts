import { GroupOption } from './GroupOption';
import { PeraGraph } from 'ng-common-library';
export declare class WarningCategoryGroupService implements GroupOption {
    /**
     * things that need to emit before generating the warning lanes
     */
    dependency: any;
    constructor();
    /**
     * generate group choice object for facilities
     */
    generateLanes(): {
        groupName: string;
        lanes: {
            laneFunction: (peraGraph: PeraGraph) => boolean;
            laneTitle: string;
        }[];
    };
}
