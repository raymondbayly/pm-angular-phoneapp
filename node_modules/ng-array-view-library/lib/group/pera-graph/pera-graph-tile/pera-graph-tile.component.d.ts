import { ChangeDetectorRef, OnChanges, OnInit } from '@angular/core';
import { PeraGraphService } from '../service/pera-graph.service';
import { WarningService } from 'ng-common-library';
import { ArrayViewService } from '../../../service/array-view.service';
export declare class PeraGraphTileComponent implements OnInit, OnChanges {
    peraGraphService: PeraGraphService;
    warningService: WarningService;
    arrayViewService: ArrayViewService;
    ref: ChangeDetectorRef;
    graph: any;
    latestRi: any;
    priorWarning: string;
    priorWarningIndicator: any;
    style: object;
    constructor(peraGraphService: PeraGraphService, warningService: WarningService, arrayViewService: ArrayViewService, ref: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnChanges(): void;
    updateBackgroundColor(): void;
}
