import { NgbPopover } from '@ng-bootstrap/ng-bootstrap';
import { Subject } from 'rxjs';
import { PeraGraph } from 'ng-common-library';
export declare class PeraGraphService {
    graphClicked$: Subject<PeraGraph>;
    tooltip: NgbPopover;
    constructor();
    graphClickedEvent(graph: PeraGraph): void;
    getBackgroundColorByWarningAndRi(warning: any, latestRi: number, isDischarged: boolean): string;
    getRiScoreToDisplay(latestRi: number): string;
    closeToolTip(): void;
}
