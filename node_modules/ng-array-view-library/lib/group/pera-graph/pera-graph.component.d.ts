import { ChangeDetectorRef, OnChanges, OnDestroy, OnInit, SimpleChanges } from '@angular/core';
import { ArrayViewService } from '../../service/array-view.service';
import { PeraGraphService } from './service/pera-graph.service';
import { NgbPopover } from '@ng-bootstrap/ng-bootstrap';
import { HttpClient } from '@angular/common/http';
import { TooltipService } from './service/tooltip.service';
import { FlowsheetsApiService, PeraGraph, UsageApiService } from 'ng-common-library';
import { PriorWarningsApiService } from '../../service/prior-warnings/api/prior-warnings-api.service';
import { FlowSheetBase } from 'ng-common-library/lib/model/FlowSheetBase';
export declare class PeraGraphComponent implements OnInit, OnDestroy, OnChanges {
    service: ArrayViewService;
    private flowsheetsApiService;
    private priorWarningsApiService;
    private usageService;
    private graphService;
    private toolTipService;
    private http;
    private ref;
    graph: PeraGraph;
    peraGraphWidth: number;
    peraGraphHeight: number;
    style: any;
    private margin;
    flowsheets: Array<FlowSheetBase>;
    priorWarning: string;
    private flowSheetMap$;
    private priorWarningMap$;
    private toolTipInfoEvent$;
    latestRi: number;
    private isMouseOverPeraGraph;
    private tooltip;
    constructor(service: ArrayViewService, flowsheetsApiService: FlowsheetsApiService, priorWarningsApiService: PriorWarningsApiService, usageService: UsageApiService, graphService: PeraGraphService, toolTipService: TooltipService, http: HttpClient, ref: ChangeDetectorRef);
    ngOnInit(): void;
    /**
     * Subscribe to the interval observables/subjects that pass information the PeraGraph needs
     *
     * Currently, this includes prior warnings and flowsheets
     */
    subscribeToPeraGraphDataFeeds(): void;
    subscribeToToolTipInfoEvent(): void;
    /**
     * This only fires when the user passes the mouse over the graph to get the tooltip.
     * For those graphs, force a change detection cycle so that the ToolTip component updates.
     */
    mouseEnterHandler(po: NgbPopover): void;
    /**
     * This fires when the user passes the mouse over the graph, and then moves the mouse off of the graph
     * For those graphs, close the tooltip
     *
     * @param po
     */
    mouseLeaveHandler(po: NgbPopover): void;
    graphClicked(graph: PeraGraph): void;
    isShown(po: NgbPopover): void;
    ngOnChanges(changes: SimpleChanges): void;
    getFlowSheetsForVisit(fscMap: any): void;
    getPriorWarningsForVisit(priorWarnings: any): void;
    ngOnDestroy(): void;
}
