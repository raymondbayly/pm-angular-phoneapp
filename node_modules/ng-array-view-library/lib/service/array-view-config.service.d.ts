import { BehaviorSubject } from 'rxjs';
export declare class ArrayViewConfigService {
    configurationLoaded$: BehaviorSubject<boolean>;
    private currentTimestamp;
    private toolTipInfoEndPoint;
    getToolTipInfoEndPoint(): string;
    setToolTipInfoEndPoint(val: string): void;
    getCurrentTimestamp(): number;
    setCurrentTimestamp(value: number): void;
}
