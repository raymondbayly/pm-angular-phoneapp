import { HttpClient } from '@angular/common/http';
import { Storage } from '@ionic/storage';
import { JwtHelperService } from '@auth0/angular-jwt';
import { TokenApiService, User } from 'ng-common-library';
import { LoginConfigService } from './login-config.service';
import { Subject } from 'rxjs';
export declare class LoginService {
    private http;
    private storage;
    private jwtHelper;
    private loginConfigService;
    private tokenService;
    private currentUser;
    login$: Subject<User>;
    hideLogin: boolean;
    readonly JWT_USER_NAME = "jwt_user";
    constructor(http: HttpClient, storage: Storage, jwtHelper: JwtHelperService, loginConfigService: LoginConfigService, tokenService: TokenApiService);
    getCurrentUser(): User;
    setCurrentUser(user: User): void;
    getLogoSrc(): string;
    getLoginApiUrl(): string;
    getLoginIpUrl(): string;
    getLoginUrl(): string;
    getLogoutUrl(): string;
    getTitle(): string;
    checkLogin(): void;
    logIn(user: User): void;
    private subAndCheckToken;
    /** Log the user out and return them to the login screen. */
    logOut(): void;
    private clearLocalUser;
}
