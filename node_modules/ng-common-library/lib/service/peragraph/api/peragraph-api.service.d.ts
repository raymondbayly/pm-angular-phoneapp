import { HttpClient } from '@angular/common/http';
import { BehaviorSubject, Observable } from 'rxjs';
import { PeraGraph } from '../../../model/PeraGraph';
import { PeragraphConfigService } from '../config/peragraph-config.service';
export declare class PeragraphApiService {
    private http;
    private peragraphConfigService;
    private readonly _peraGraphSubject;
    private pollInstance$;
    constructor(http: HttpClient, peragraphConfigService: PeragraphConfigService);
    getPeraGraphSubject(): BehaviorSubject<PeraGraph[]>;
    getSinglePeragraph(): Observable<PeraGraph>;
    getAllPeragraphs(): Observable<any>;
    getPeraGraphInterval(): Observable<any>;
}
