import { HttpClient } from '@angular/common/http';
import { User } from '../../../model/User';
import { TokenConfigService } from '../config/token-config.service';
import { Storage } from '@ionic/storage';
import { JwtHelperService } from '@auth0/angular-jwt';
import { Subject } from 'rxjs';
export declare class TokenApiService {
    private http;
    private jwtHelper;
    private config;
    private storage;
    private jwtTokenName;
    private appId;
    private deviceToken;
    readonly token$: Subject<string>;
    constructor(http: HttpClient, jwtHelper: JwtHelperService, config: TokenConfigService, storage: Storage);
    /**
     * Check the current token.
     */
    checkToken(): void;
    /**
     * Refresh the token for the given user.
     *
     * @param user User for which to refresh token.
     */
    refreshToken(user: User): void;
    getToken(user: User): void;
    clearToken(): void;
    getExistingToken(): void;
    getAppId(): string;
}
